# 硅基流动语音扩展

这是一个 SillyTavern 第三方扩展原型：

- 在每条消息旁边添加一个“硅基流动语音”按钮
- 点击后朗读该条消息
- 当前版本通过硅基流动 TTS API 生成 mp3 并在前端播放

## 当前限制

当前版本依赖：

1. 需要填写可用的硅基流动 API Key
2. 长文本生成会有等待时间
3. 不同模型支持的 voice 可能存在差异，需要按实际接口表现微调

当前实现更适合移动端，因为它走的是普通 HTTP TTS API。